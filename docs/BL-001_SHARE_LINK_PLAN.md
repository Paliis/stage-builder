# BL-001 — план дій: посилання на вправу (share link)

Короткий URL замість (або поруч із) завантаження `*.stage.json`. **Статус:** план реалізації; після затвердження оновлювати цей файл фазами.

**Пов’язано:** [BACKLOG.md](./BACKLOG.md) **BL-001**; деплой — Vercel (`vercel.json`); формат сцени — `stageProjectFile.ts` / `*.stage.json`.

---

## Прийняті рішення (продукт)

*Оновлювати при зміні політики.*

| Тема | Рішення |
|------|---------|
| **Логін** | Поки **без логіну**; створення посилань **анонімно**. |
| **Модель довіри** | **Прийнятна:** хто має URL — може відкрити вправу; список «мої посилання» **не** в MVP. |
| **Квота створення** | **50 нових посилань на добу** на одного клієнта (для аноніму — орієнтир: **per IP**, уточнити в реалізації з урахуванням NAT/IPv6). |
| **Перевищення квоти** | Показувати **капчу** (наприклад Cloudflare Turnstile або hCaptcha) і після успіху дозволяти подальше створення в межах розумного; деталі інтеграції — у задачі на бекенд. |
| **Термін життя посилання** | За замовчуванням **365 днів** з моменту створення (`expires_at`); безстроковість і продовження — не в MVP, за потреби пізніше. |

**Відкрито для реалізації:** відновлення доступу до редагування без акаунта (edit token у відповіді POST — збереження на стороні клієнта).

---

## 0. Рішення продукту (закрити до коду)

Нижче — **розгорнутий перелік питань**, на які варто дати явні відповіді (навіть якщо «поки ні» або «потім»). Частину вже закрито в таблиці **«Прийняті рішення»** вище.

### 0.1. Хто і як створює посилання

- **Ідентифікація автора:** ~~чи достатньо анонімного створення~~ → **так, без логіну (MVP).** Акаунти — пізніше, якщо знадобиться.
- **Якщо анонімно:** **модель «хто знає посилання — бачить вправу»** прийнята; списку «мої посилання» немає — як користувачу **відновити** доступ до редагування, якщо загубив **edit token** — окреме питання (покластися на збережений токен / нове посилання).
- **Квоти:** **50 / доба**, перевищення → **капча** (див. таблицю вище).
- **Термін життя:** **365 днів** за замовчуванням (див. таблицю вище).

### 0.2. Що саме бачить отримувач посилання (режими)

- **Режим «стрілки» (view-only):** чи обов’язково **заборонити будь-які зміни** сцени в UI (лише гортання 2D/3D, читання брифінгу), чи дозволити **локальні** зміни «на кшталт редактора», але без кнопки «зберегти в хмару» і з чітким банером «це несохранені зміни»?
- **Режим «організатор»:** чи потрібен **окремий** тип посилання, який відкриває **повний редактор** (як зараз при відкритті файлу), чи достатньо одного посилання для всіх, а режим «тільки перегляд» задається **при створенні** (один URL = один режим)?
- **Перемикання режиму:** чи допустимо мати **два** посилання на одну й ту саму вправу (одне для стрілків, одне для суддів) — тоді в БД два записи або один запис з полем `default_mode`?
- **Чернетка в браузері:** якщо користувач відкрив посилання в режимі перегляду, а потім натиснув «Редагувати в редакторі» — чи це **нова вкладка** з повним режимом, чи **модальне** попередження з копією в локальний стан?

### 0.3. Оновлення та версії на сервері

- **Один id назавжди:** чи організатор має право **оновити** вправу на сервері, **не змінюючи** URL? Якщо так — як видається **секрет редагування** (одноразовий токен при створенні, збережений у користувача; повторна генерація з email)?
- **Новий id при кожній публікації:** чи простіше продуктово завжди створювати **нове** посилання після зміни сцени (старі посилання лишаються на старій версії)? Як це пояснювати в UI («попередній лінк залишиться на старій версії»)?
- **Історія версій:** чи потрібна **історія** змін на сервері (навіть як 3–5 останніх snapshot’ів), чи достатньо лише «останній payload»?
- **Видалення:** чи може автор **видалити** вправу з сервера (посилання перестає працювати)? Як це поєднується з акаунтом vs анонімним токеном?

### 0.4. Приватність, контент і відповідальність

- **Публічність за замовчуванням:** чи всі посилання вважаються **публічними** (індексація пошуковиками — зазвичай `noindex` для `/s/:id`)? Чи явно потрібен мета-тег **noindex** для сторінок шарингу?
- **Пароль з посиланням:** чи потрібен **окремий** тип «посилання + пароль» для внутрішніх розсилок клубу (MVP чи пізніше)?
- **Персональні дані в брифінгу:** чи є ризик, що користувачі внесуть в брифінг **ПІБ** / телефони? Чи потрібен **дисклеймер** при створенні посилання («не передавайте персональні дані») або згода з політикою?
- **Скарги/аб’юз:** чи потрібен **контакт** (email) для скарг на неприйнятний контент у збереженій вправі? Хто модератор (вручну власник продукту)?

### 0.5. Інтеграція з поточним продуктом

- **Файл vs посилання:** чи **замінюємо** експорт файлу для частини сценаріїв, чи **завжди** лишаємо «Завантажити JSON» поруч із «Поділитися»?
- **Чернетка в `localStorage`:** якщо відкрили посилання з уже існуючою чернеткою — чи **перезаписуємо** чернетку, чи показуємо діалог «зберегти попередню / відкинути»?
- **QR у PDF:** чи в QR у брифінгу за замовчуванням має бути **посилання на share**, а не лише головна сторінка застосунку? Чи два варіанти на вибір при експорті?

### 0.6. Мова, бренд, домен

- **Мова UI:** чи посилання **нейтральне** (мова з `localStorage` / браузера), чи вшиваємо `?lang=uk` для офіційних матчів?
- **Короткий домен:** чи потрібен **окремий** короткий домен (редирект на `stage-builder.vercel.app/s/...`), чи достатньо довгого шляху на поточному домені?
- **Назва вправи в OG:** чи генеруємо **Open Graph** прев’ю для сторінки `/s/:id` (назва вправи, зображення) — це потребує SSR або окремої сторінки для ботів?

---

## 1. Архітектура даних (Supabase або аналог)

| Елемент | Опис |
|---------|------|
| **Таблиця `shared_stages`** (назва умовна) | `id` (short public id, напр. nanoid 8–10 символів), `payload` (JSONB — тіло проєкту як у файлі, або нормалізований об’єкт), `created_at`, `expires_at` (nullable), `edit_token_hash` (nullable — bcrypt артефакту для оновлення), `schema_version` |
| **RLS** | Політики: `INSERT` для anon (з обмеженнями), `SELECT` за `id`, `UPDATE` лише з валідним edit token (через Edge Function, не прямий anon update) |
| **Розмір** | Обмежити розмір JSON (наприклад ≤ 512 KB) на рівні API |

Альтернатива без власного API: **Supabase Storage** (один JSON на об’єкт) + метадані в таблиці — зручно для великих файлів; для MVP часто достатньо **одного JSONB** у Postgres.

---

## 2. Backend / API (мінімальний контур)

1. **POST `/api/share`** (Vercel Serverless або Supabase Edge Function)
   - Body: JSON сцени (той самий формат, що `*.stage.json`).
   - Відповідь: `{ id, url, editToken? }` — `editToken` показувати **один раз**, якщо плануєте оновлення.
2. **GET `/api/share/:id`** (або лише клієнт через Supabase client `select`)
   - Повертає JSON для гідрації стору.
3. **PATCH** (опційно) — оновлення за `id` + `Authorization: Bearer <editToken>`.
4. **Rate limiting** — Vercel Edge Middleware або ліміт у функції (per IP + sliding window).

Якщо використовуєте **лише Supabase клієнт з ключа anon** у браузері — RLS має бути безпечним; **service role** — лише на сервері, ніколи в клієнті.

---

## 3. Фронтенд (React SPA)

1. **Маршрутизація**
   - Додати **React Router** (або легкий варіант: `useEffect` у `App` за `location.pathname` / `searchParams`) для шляхів на кшталт `/s/:id` або `/v/:id`.
   - **Vercel:** `rewrites` для SPA — усі `/*` → `index.html` (якщо ще не налаштовано автоматично для Vite).

2. **Потік «Відкрити за посиланням»**
   - При завантаженні `/s/:id`: `fetch` → `parseStageProjectJson` → `hydrate` у store (аналогічно Open file).
   - Режим **view:** прапорці в сторі або в URL — вимкнути запис на сервер, опційно приховати кнопки «зберегти в хмару» / залишити лише «Експорт PDF» / «Завантажити JSON».

3. **Потік «Поділитися»**
   - Кнопка поруч із Save/Open: «Отримати посилання» → POST → показати URL + кнопка копіювання, опційно QR (як у PDF — узгодити з `exportBriefingPdf` / `APP_URL`).

4. **Змінні середовища**
   - `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (для клієнта); секрети — у Vercel env для функцій.

---

## 4. Безпека та зловживання

- Ліміт розміру тіла + кількість створень на IP/годину.
- **CORS** — лише ваш домен для браузерного клієнта, якщо API окремий.
- **Не** логувати повний JSON у публічні логи без потреби.
- Опційно: **report abuse** / видалення за скаргою (пізніше).

---

## 5. Документація та реліз

- Оновити **FUNCTIONALITY.md** (розділ файли / share).
- **TECH.md** — схема БД, env, маршрути.
- **VERSIONING.md** — короткий рядок для користувачів (V2.x).
- Оновити **BACKLOG.md**: статус BL-001 → `in progress` / `done` після фаз.

---

## 6. Фази виконання (порядок)

| Фаза | Зміст | Результат |
|------|--------|-----------|
| **1** | Продуктові відповіді з §0; вибір Supabase vs лише Vercel Functions + Postgres | рішення зафіксовано |
| **2** | Проєкт Supabase: таблиця, RLS, тестові INSERT/SELECT з SQL | дані живуть у хмарі |
| **3** | POST/GET API (Edge або Serverless) + тести з curl | контракт стабільний |
| **4** | UI: «Поділитися» + сторінка/режим відкриття за `/s/:id` | MVP для користувача |
| **5** | Режим view-only + i18n рядки | стрілки без ризику зміни опублікованої сцени |
| **6** | (Опційно) edit token, оновлення посилання | організатори оновлюють ту саму URL |
| **7** | QR у PDF на share link; аналітика подій (опційно) | повний цикл |

**Оцінка:** фази 1–4 ≈ **L** (1–2 тижні залежно від досвіду з Supabase/Auth); додавання акаунтів і модерації — окремий інкремент.

---

## 7. Додаткові відкриті питання (техніка та експлуатація)

### 7.1. Інфраструктура

- **Постачальник БД:** чи остаточно **Supabase** (Postgres + Auth + RLS), чи розглядаємо **Vercel Postgres + Serverless** без Supabase? Критерії: вартість на старті, ліміти безкоштовного рівня, зручність RLS.
- **Регіон даних:** чи є вимога зберігати дані **в ЄС** (GDPR) для частини користувачів — чи достатньо вибору регіону Supabase при створенні проєкту?
- **Резервне копіювання:** чи потрібен **експорт** усіх опублікованих вправ для власника (на випадок міграції з платформи)?

### 7.2. API та контракт

- **Версія формату файлу:** чи при збереженні в хмару **завжди** нормалізуємо до останньої `STAGE_PROJECT_VERSION`, чи приймаємо старі версії як при відкритті файлу?
- **Частковий payload:** чи можна зберігати лише **частину** полів (наприклад без чернетки брифінгу) — щоб зменшити розмір — чи завжди **повний** знімок як у `*.stage.json`?
- **Ідемпотентність POST:** чи потрібен **клієнтський** ключ запиту (request id) для повтору при обриві мережі, щоб не створити дублікати посилань?

### 7.3. Спостережуваність

- **Аналітика переходів:** чи збираємо лише **агрегувати** (кількість відкриттів по `id` без fingerprint), чи достатньо подій у **GA4** / Vercel Analytics на клієнті?
- **Логи сервера:** на скільки днів зберігати логи помилок API (rate limit, 413 payload too large)?

### 7.4. Доступність і офлайн

- **PWA:** чи після відкриття посилання вправу можна **відкрити офлайн** з кешу (як зараз локальний файл), чи share — **завжди онлайн**?
- **Часовий зсув:** чи важливо показувати **час створення** посилання в UI для організатора?

### 7.5. Юридичне та підтримка

- **Умови використання:** чи потрібно оновити **Terms** / Privacy Policy щодо хостингу контенту користувачів та посилань?
- **DMCA / видалення:** чи описуємо процедуру **видалення** вправи за зверненням (email шаблон)?

### 7.6. Промо та якість

- **Брендований короткий домен** (окремий DNS / redirect на основний домен) — чи це must-have для маркетингу чи можна відкласти?
- **Прев’ю в месенджерах:** чи потрібні **og:image** з мініатюрою плану (складно без SSR) — чи достатньо текстового заголовка вправи?

---

**Після відповідей** на пункти §0 і §7 — зафіксувати рішення в таблиці або в окремому документі «BL-001 — рішення», оновити цей план і розбити на задачі в трекері (GitHub Issues / Projects).
